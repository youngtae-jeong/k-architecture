/*
  jquery.scrolly.min.js (stub)
  Template references jQuery Scrolly but the original file was missing, causing 404.
  Scrolly isn't required for the News click-to-toggle UI, so this safe stub no-ops.
*/
(function($){
  if(!$) return;
  $.fn.scrolly = $.fn.scrolly || function(){ return this; };
})(window.jQuery);
